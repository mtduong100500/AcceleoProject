<?xml version="1.0" encoding="UTF-8"?>
<project default="generate" name="org.eclipse.acceleo.module.mid">
    <property name="ECLIPSE_HOME" value="C:/Users/Duong/eclipse/java-2020-12/eclipse"/>
    <property name="ECLIPSE_WORKSPACE" value="C:/Users/Duong/eclipse-workspace"/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="org.eclipse.acceleo.module.mid.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/org.eclipse.acceleo.module.mid/bin"/>
    </path>

    <path id="org.eclipse.acceleo.module.mid.classpath">
        <path refid="org.eclipse.acceleo.module.mid.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="org.eclipse.acceleo.module.mid.libraryclasspath.alljars">
        <pathelement location="../org.eclipse.acceleo.module.mid/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="org.eclipse.acceleo.module.mid.classpath.alljars">
        <path refid="org.eclipse.acceleo.module.mid.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="generate">
        <java classname="org.eclipse.acceleo.module.mid.main.Generate" classpathref="org.eclipse.acceleo.module.mid.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="generate.alljars">
        <java classname="org.eclipse.acceleo.module.mid.main.Generate" classpathref="org.eclipse.acceleo.module.mid.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
